# updating release/1.3.0 after commit on master and release/1.4.0 created
stages:
  - build
# tada!
# is this actually working?
# yay I think it is actually working!!
variables:
  LC_ALL: "en_US.UTF-8"
  LANG: "en_US.UTF-8"
  GIT_STRATEGY: clone

build:
  stage: build
  script:
    - uname -a
    - ls -al
    # - bundle install
    # - gem uninstall fastlane --all --executables
    # - gem install fastlane --version 2.212.0 --no-document
    # - bundle update
    # - fastlane --version
    # - bundler --version
    # - bundle exec fastlane build_upload_testflight --verbose
  artifacts:
    paths:
      - CarmenGrid.ipa
      - codemagic.yaml
